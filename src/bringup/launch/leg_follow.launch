<launch>

    <include file="$(find people_velocity_tracker)/launch/tracked_detector.launch" />

    <!-- display the marker of person in rviz(without filter) -->
    <node name="rviz_leg" pkg="rviz" type="rviz" args="-d /home/john/.rviz/leg.rviz" />

    <!--
    <node name="follow" pkg="people_velocity_tracker" type="follow.py" />
	<node name="sound_master" pkg="sound_play" type="soundplay_node.py" output="screen" />
    -->
</launch>
